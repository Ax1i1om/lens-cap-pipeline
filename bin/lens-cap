#!/usr/bin/env python3
"""Repository-local launcher (works before installing the package)."""

from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from lens_cap_pipeline.cli import main  # noqa: E402

raise SystemExit(main())
