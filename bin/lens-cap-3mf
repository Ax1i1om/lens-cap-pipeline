#!/bin/sh
# SPDX-License-Identifier: Apache-2.0
# Repository-local launcher; keep it usable before an editable install exists.
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec python3 "$ROOT/scripts/build_3mf.py" "$@"
